class Solution:
    def getRow(self, rowIndex: int) -> List[int]:
        import math
        row = []
        for i in range(rowIndex+1):
            row.append(math.comb(rowIndex,i))

        return row
